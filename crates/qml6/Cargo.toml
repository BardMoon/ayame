[package]
name = "ayame"
links = "ayame"
version.workspace = true
edition.workspace = true

[dependencies]
ayame-colors = { path = "../colors" }
ayame-config = { path = "../config" }
ayame-icons = { path = "../icons" }

#[ Crates.io ]
cxx = { workspace = true }
cxx-qt = { workspace = true }
cxx-qt-lib = { workspace = true, features = ["qt_full"] }

[build-dependencies]
cxx-qt-build = { workspace = true }
